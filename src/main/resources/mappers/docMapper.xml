<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.docMapper">
	
	<select id="selectNormalDocList" resultType="map">
		select 문서명 docName, 파일명  fileName, 확장자 ext
		  from [동림].[dbo].[공용일반문서]
	</select>	
	
</mapper>
